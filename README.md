🔐 Test Accounts

Use the following accounts to log in and test the system:

👨‍⚕️ Doctor Account

Username: doctor

Password: doctor

🧑‍🦱 Patient Account

Username: patient

Password: patient

⚙️ System Requirements & Dependencies

Before running the Clinic Appointment System, make sure the following are installed on your machine:

🐍 Python

Python 3.13 

🖥️ Interface (GUI)

This system uses PyQt6 for the graphical user interface.

Install it using:

pip install PyQt6

🗄️ Database

The system uses MySQL for data storage.

Required:

MySQL Server

MySQL Connector for Python

Install the connector:

pip install mysql-connector-python


Make sure to:

Import the provided .sql file into your MySQL database

Update database credentials in the Python configuration file if needed

📊 Reports & Charts

The system uses Matplotlib for reports and KPI visualizations.

Install it using:

pip install matplotlib

▶️ How to Run the System

Clone the repository:

git clone https://github.com/lalicaya557990-svg/CareLine_Clinic_Appointment_System.git


Navigate to the project folder:

cd Clinic_Appointment_System


Make sure all dependencies are installed

Run the main application file:

main_app.py

📌 Notes

Ensure MySQL is running before launching the application

This project is for academic purposes / GitHub validation

Sample accounts are provided for testing only
